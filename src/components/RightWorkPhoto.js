//react
import React from "react";

const RightWorkPhoto = ({ src }) => {
  return (
    <section className="rightDetails">
      <img src={src} alt="" className="workPhoto" />
    </section>
  );
};

export default RightWorkPhoto;
